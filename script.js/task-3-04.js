/*const countTotalSalary = function (employees) {
  'use strict';
  // Write code under this line
  let sum = 0; //cоздаем переменную для суммы зарплат со значением (0)

  for (let Salary of Object.values(employees)) {
    // новую переменную(ЗАРПЛАТА) перебираем из (МАССИВА ЗНАЧЕНИЙ)
    sum += Salary; //в общую сумму складываем сумму + каждый раз зарплату с каждого перебора
  }
  return sum; //возвращаем сумму
};

// Объекты и ожидаемый результат
const developers = {
  mango: 300,
  poly: 250,
  alfred: 450,
};
console.log(countTotalSalary(developers));
// 1000

const supports = {
  kiwi: 200,
  lux: 150,
  chelsy: 150,
};
console.log(countTotalSalary(supports));
// 500
*/
